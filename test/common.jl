# Shared test helpers.
#
# (The old `validate_serialization` helper — which exercised the retired `to_json` /
# `Portfolio(path)` serialization — was removed; OpenAPI document round-trips are covered by
# `test_serialization.jl`'s `to_file`/`from_file` testset.)
